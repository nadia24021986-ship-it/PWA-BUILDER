```html
<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portal Kehadiran</title>
    <!-- Tailwind CSS dari CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @media print {
            .print-hidden { display: none !important; }
            body { background: white; padding: 20px; }
        }
    </style>
</head>
<body class="bg-slate-50 text-slate-800">
    <div id="app">
        <!-- Konten akan dimuat oleh JavaScript -->
        <div class="flex items-center justify-center h-screen">Memuat aplikasi...</div>
    </div>

    <script>
        // Logika aplikasi sederhana (Vanilla JS)
        // Karena kode asli Anda adalah React, ini adalah versi sederhana agar jalan di HTML murni
        document.getElementById('app').innerHTML = `
            <div class="p-10 text-center">
                <h1 class="text-3xl font-bold">Portal Kehadiran</h1>
                <p class="mt-4 text-slate-600">Sistem ini memerlukan lingkungan React untuk berjalan.</p>
                <p class="mt-2 text-sm text-slate-400">Jika Anda ingin menggunakan file tunggal HTML, 
                   Anda perlu menulis ulang logika komponen React Anda ke fungsi-fungsi JavaScript standar 
                   yang memanipulasi DOM langsung (document.getElementById, dll).</p>
            </div>
        `;
    </script>
</body>
</html>

```
