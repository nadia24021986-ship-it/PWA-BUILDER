06.23 21.16
```html
<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PWA Builder Pro - Pembuat & Penyimpan Aplikasi Offline</title>
    <!-- Tailwind CSS untuk styling -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- JSZip untuk membuat file .zip -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
    <!-- FileSaver untuk mengunduh file .zip -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/FileSaver.js/2.0.5/FileSaver.min.js"></script>
    <!-- Dexie.js untuk mempermudah penggunaan database IndexedDB -->
    <script src="https://unpkg.com/dexie@3.2.4/dist/dexie.js"></script>
    <style>
        textarea::-webkit-scrollbar { width: 8px; }
        textarea::-webkit-scrollbar-track { background: #f1f1f1; border-radius: 4px; }
        textarea::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 4px; }
        textarea::-webkit-scrollbar-thumb:hover { background: #94a3b8; }
    </style>
</head>
<body class="bg-slate-50 min-h-screen font-sans text-slate-800 pb-12">
    <!-- Header -->
    <header class="bg-indigo-600 text-white shadow-md">
        <div class="container mx-auto px-4 py-6">
            <div class="flex justify-between items-center flex-wrap gap-4">
                <div>
                    <h1 class="text-3xl font-bold flex items-center gap-2">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z" />
                        </svg>
                        PWA Builder <span class="text-xs bg-indigo-500 px-2 py-1 rounded-full font-normal">v3.0 - Database Edition</span>
                    </h1>
                    <p class="mt-2 text-indigo-100">Buat, simpan, arsipkan, dan download kembali aplikasi HP offline Anda kapan saja.</p>
                </div>
            </div>
        </div>
    </header>
    <main class="container mx-auto px-4 py-8">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- Kolom Pengaturan Aplikasi -->
            <div class="lg:col-span-1 space-y-6">
                <div class="bg-white p-6 rounded-xl shadow-sm border border-slate-200">
                    <h2 class="text-xl font-semibold mb-4 text-slate-700 border-b pb-2">Pengaturan Aplikasi</h2>
                    <div class="space-y-4">
                        <div>
                            <label class="block text-sm font-medium text-slate-600 mb-1">Nama Aplikasi</label>
                            <input type="text" id="appName" value="Aplikasi Kerenku" class="w-full px-4 py-2 border border-slate-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all">
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-slate-600 mb-1">Nama Singkat (Ikon HP)</label>
                            <input type="text" id="appShortName" value="AppKeren" class="w-full px-4 py-2 border border-slate-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all">
                        </div>
                        <!-- FITUR BARU: Nama File ZIP Kustom -->
                        <div>
                            <label class="block text-sm font-medium text-slate-600 mb-1">Nama File ZIP Output</label>
                            <div class="flex">
                                <input type="text" id="zipName" value="app-keren-v1" class="w-full px-4 py-2 border border-slate-300 rounded-l-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all">
                                <span class="inline-flex items-center px-3 rounded-r-lg border border-l-0 border-slate-300 bg-slate-100 text-slate-500 text-sm">.zip</span>
                            </div>
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-slate-600 mb-1">Logo Aplikasi (PNG/JPG)</label>
                            <input type="file" id="appLogo" accept="image/png, image/jpeg" class="w-full text-sm text-slate-500 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-semibold file:bg-indigo-50 file:text-indigo-700 hover:file:bg-indigo-100 cursor-pointer border border-slate-300 rounded-lg p-1">
                            <p class="text-xs text-slate-400 mt-1">*Jika kosong, otomatis memakai inisial nama.</p>
                        </div>
                        <div class="grid grid-cols-2 gap-4">
                            <div>
                                <label class="block text-sm font-medium text-slate-600 mb-1">Warna Tema</label>
                                <input type="color" id="themeColor" value="#4f46e5" class="w-full h-10 rounded-lg cursor-pointer border border-slate-300">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-slate-600 mb-1">Warna Ikon *</label>
                                <input type="color" id="iconColor" value="#ffffff" class="w-full h-10 rounded-lg cursor-pointer border border-slate-300">
                            </div>
                        </div>
                    </div>
                </div>
                <!-- Petunjuk Database -->
                <div class="bg-indigo-50 p-6 rounded-xl border border-indigo-100 text-sm text-indigo-800 space-y-2">
                    <h3 class="font-semibold text-indigo-900 flex items-center gap-1">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4m0 5c0 2.21-3.582 4-8 4s-8-1.79-8-4" />
                        </svg>
                        Penyimpanan Aman (Offline DB):
                    </h3>
                    <p>Semua file ZIP tersimpan di database lokal browser perangkat ini. Anda tidak butuh login dan tidak akan kehilangan data meskipun halaman di-refresh!</p>
                </div>
            </div>
            <!-- Kolom Input Kode -->
            <div class="lg:col-span-2 space-y-6">
                <div class="bg-white p-6 rounded-xl shadow-sm border border-slate-200">
                    <h2 class="text-xl font-semibold mb-4 text-slate-700 border-b pb-2">Kode Sumber Aplikasi</h2>
                    <div class="space-y-4">
                        <div>
                            <label class="flex justify-between items-center text-sm font-medium text-slate-600 mb-1">
                                <span>HTML (Bagian dalam tag &lt;body&gt;)</span>
                                <span class="text-xs bg-slate-100 px-2 py-1 rounded text-slate-500">index.html</span>
                            </label>
                            <textarea id="codeHtml" rows="5" class="w-full p-4 font-mono text-sm border border-slate-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all bg-slate-50" placeholder="<h1>Halo Dunia</h1>..."></textarea>
                        </div>
                        <div>
                            <label class="flex justify-between items-center text-sm font-medium text-slate-600 mb-1">
                                <span>CSS (Gaya Tampilan)</span>
                                <span class="text-xs bg-slate-100 px-2 py-1 rounded text-slate-500">style.css</span>
                            </label>
                            <textarea id="codeCss" rows="4" class="w-full p-4 font-mono text-sm border border-slate-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all bg-slate-50" placeholder="body { ... }"></textarea>
                        </div>
                        <div>
                            <label class="flex justify-between items-center text-sm font-medium text-slate-600 mb-1">
                                <span>JavaScript (Logika Aplikasi)</span>
                                <span class="text-xs bg-slate-100 px-2 py-1 rounded text-slate-500">app.js</span>
                            </label>
                            <textarea id="codeJs" rows="4" class="w-full p-4 font-mono text-sm border border-slate-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all bg-slate-50" placeholder="console.log('Aktif');"></textarea>
                        </div>
                    </div>
                </div>
                <!-- Tombol Eksekusi -->
                <button id="generateBtn" class="w-full py-4 bg-indigo-600 hover:bg-indigo-700 text-white text-lg font-bold rounded-xl shadow-lg hover:shadow-xl transition-all transform hover:-translate-y-1 flex justify-center items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 4H6a2 2 0 00-2 2v12a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-2m-4-1v8m0 0l3-3m-3 3L9 8m-5 5h2.586a1 1 0 01.707.293l2.414 2.414a1 1 0 00.707.293h3.172a1 1 0 00.707-.293l2.414-2.414a1 1 0 01.707-.293H20" />
                    </svg>
                    Buat, Simpan & Download PWA (.zip)
                </button>
                <div id="notification" class="hidden mt-4 p-4 rounded-lg text-center font-medium transition-all"></div>
            </div>
        </div>
        <!-- FITUR BARU: PANEL ARSIP & RIWAYAT PROYEK -->
        <div class="mt-12 bg-white p-6 rounded-xl shadow-sm border border-slate-200">
            <h2 class="text-2xl font-bold mb-4 text-slate-700 flex items-center gap-2">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-indigo-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
                </svg>
                Arsip Aplikasi yang Tersimpan
            </h2>
            <p class="text-sm text-slate-500 mb-6">Berikut adalah daftar seluruh file PWA yang pernah Anda buat di browser ini. Anda bisa mendownload kembali file ZIP-nya kapan saja.</p>
            <div class="overflow-x-auto">
                <table class="w-full text-left border-collapse">
                    <thead>
                        <tr class="bg-slate-100 text-slate-700 uppercase text-xs font-semibold">
                            <th class="p-4 rounded-l-lg">Logo</th>
                            <th class="p-4">Nama Aplikasi</th>
                            <th class="p-4">Nama File (.zip)</th>
                            <th class="p-4">Tanggal Dibuat</th>
                            <th class="p-4 rounded-r-lg text-right">Aksi</th>
                        </tr>
                    </thead>
                    <tbody id="projectTableBody" class="divide-y divide-slate-100 text-sm">
                        <!-- Data akan dimasukkan otomatis lewat JavaScript -->
                        <tr>
                            <td colspan="5" class="p-8 text-center text-slate-400">Belum ada aplikasi yang disimpan. Buat aplikasi pertama Anda di atas!</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </main>
    <script>
        // --- 1. INISIALISASI DATABASE LOKAL (INDEXEDDB) ---
        // Kita menggunakan Dexie.js agar manajemen penyimpanan blob ZIP menjadi sangat mudah
        const db = new Dexie("PWABuilderDatabase");
        db.version(1).stores({
            projects: "++id, appName, zipName, dateCreated, logoBase64, zipBlob"
        });
        // --- 2. SET DATA DEFAULT ---
        document.getElementById('codeHtml').value = `<div class="container">\n  <h1>Arsip Offline Sukses!</h1>\n  <p>Data aplikasi ini sekarang tersimpan secara permanen di database lokal browser.</p>\n  <button onclick="alert('Halo dari PWA!')">Tes Logika</button>\n</div>`;
        document.getElementById('codeCss').value = `body { font-family: system-ui, -apple-system, sans-serif; display: flex; justify-content: center; align-items: center; height: 100vh; margin: 0; background-color: #f8fafc; }\n.container { text-align: center; background: white; padding: 2.5rem; border-radius: 16px; box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05); border: 1px solid #e2e8f0; }\nbutton { background: #4f46e5; color: white; border: none; padding: 12px 24px; border-radius: 8px; cursor: pointer; font-size: 16px; margin-top: 15px; font-weight: 600; }\nbutton:hover { background: #4338ca; }`;
        document.getElementById('codeJs').value = `console.log('Database terintegrasi dengan sempurna.');`;
        // Update otomatis nama file zip berdasarkan nama singkat
        document.getElementById('appShortName').addEventListener('input', (e) => {
            const formatted = e.target.value.toLowerCase().replace(/[^a-z0-9]/g, '-');
            document.getElementById('zipName').value = formatted ? `${formatted}-v1` : 'app-v1';
        });
        // --- 3. GENERATOR LOGO INISIAL ---
        function generateIconBase64(text, bgColor, textColor) {
            const canvas = document.createElement('canvas');
            canvas.width = 512;
            canvas.height = 512;
            const ctx = canvas.getContext('2d');
            ctx.fillStyle = bgColor;
            ctx.fillRect(0, 0, 512, 512);
            ctx.fillStyle = textColor;
            ctx.font = 'bold 250px sans-serif';
            ctx.textAlign = 'center';
            ctx.textBaseline = 'middle';
            ctx.fillText(text.charAt(0).toUpperCase(), 256, 276); 
            return canvas.toDataURL('image/png').split(',')[1]; 
        }
        function readLogoAsBase64(file) {
            return new Promise((resolve, reject) => {
                const reader = new FileReader();
                reader.onload = (event) => resolve(event.target.result.split(',')[1]);
                reader.onerror = (err) => reject(err);
                reader.readAsDataURL(file);
            });
        }
        // --- 4. RENDER DAFTAR ARSIP DARI DATABASE ---
        async function loadArchiveTable() {
            const tbody = document.getElementById('projectTableBody');
            const projects = await db.projects.toArray();
            if (projects.length === 0) {
                tbody.innerHTML = `
                    <tr>
                        <td colspan="5" class="p-8 text-center text-slate-400">Belum ada aplikasi yang disimpan. Buat aplikasi pertama Anda di atas!</td>
                    </tr>
                `;
                return;
            }
            // Urutkan dari yang terbaru
            projects.reverse();
            tbody.innerHTML = '';
            projects.forEach(project => {
                const tr = document.createElement('tr');
                tr.className = "hover:bg-slate-50 transition-colors";
                // Format Tanggal
                const date = new Date(project.dateCreated);
                const formattedDate = date.toLocaleDateString('id-ID', {
                    day: 'numeric', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit'
                });
                tr.innerHTML = `
                    <td class="p-4">
                        <img src="data:image/png;base64,${project.logoBase64}" class="w-10 h-10 rounded-lg shadow-inner object-cover" alt="logo">
                    </td>
                    <td class="p-4 font-semibold text-slate-700">${project.appName}</td>
                    <td class="p-4 text-slate-500 font-mono text-xs">${project.zipName}.zip</td>
                    <td class="p-4 text-slate-500">${formattedDate}</td>
                    <td class="p-4 text-right space-x-2">
                        <button onclick="downloadFromArchive(${project.id})" class="inline-flex items-center gap-1 bg-indigo-50 hover:bg-indigo-100 text-indigo-700 px-3 py-1.5 rounded-lg text-xs font-semibold transition-colors">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M3 17a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm3.293-7.707a1 1 0 011.414 0L9 10.586V3a1 1 0 112 0v7.586l1.293-1.293a1 1 0 111.414 1.414l-3 3a1 1 0 01-1.414 0l-3-3a1 1 0 010-1.414z" clip-rule="evenodd" />
                            </svg>
                            Download
                        </button>
                        <button onclick="deleteFromArchive(${project.id})" class="inline-flex items-center gap-1 bg-red-50 hover:bg-red-100 text-red-700 px-3 py-1.5 rounded-lg text-xs font-semibold transition-colors">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M9 2a1 1 0 00-.894.553L7.382 4H4a1 1 0 000 2v10a2 2 0 002 2h8a2 2 0 002-2V6a1 1 0 100-2h-3.382l-.724-1.447A1 1 0 0011 2H9zM7 8a1 1 0 012 0v6a1 1 0 11-2 0V8zm5-1a1 1 0 00-1 1v6a1 1 0 102 0V8a1 1 0 00-1-1z" clip-rule="evenodd" />
                            </svg>
                            Hapus
                        </button>
                    </td>
                `;
                tbody.appendChild(tr);
            });
        }
        // --- 5. OPERASI DOWNLOAD DARI DATABASE ---
        async function downloadFromArchive(id) {
            const project = await db.projects.get(id);
            if (project) {
                saveAs(project.zipBlob, `${project.zipName}.zip`);
            }
        }
        // --- 6. OPERASI HAPUS DARI DATABASE ---
        async function deleteFromArchive(id) {
            if (confirm("Apakah Anda yakin ingin menghapus arsip aplikasi ini dari memori browser?")) {
                await db.projects.delete(id);
                loadArchiveTable();
            }
        }
        // --- 7. PROSES UTAMA: GENERATE & SIMPAN PWA ---
        document.getElementById('generateBtn').addEventListener('click', async () => {
            const btn = document.getElementById('generateBtn');
            const notif = document.getElementById('notification');
            try {
                btn.innerHTML = `<svg class="animate-spin -ml-1 mr-3 h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg> Sedang Menyimpan & Meracik...`;
                btn.disabled = true;
                const appName = document.getElementById('appName').value || 'My PWA App';
                const appShortName = document.getElementById('appShortName').value || 'PWA App';
                let zipName = document.getElementById('zipName').value || 'app-v1';
                // Hindari ekstensi ganda jika pengguna tidak sengaja mengetik .zip
                zipName = zipName.replace(/\.zip$/i, ''); 
                const themeColor = document.getElementById('themeColor').value;
                const iconColor = document.getElementById('iconColor').value;
                const logoFile = document.getElementById('appLogo').files[0];
                const userHtml = document.getElementById('codeHtml').value;
                const userCss = document.getElementById('codeCss').value;
                const userJs = document.getElementById('codeJs').value;
                const cacheName = appName.replace(/[^a-zA-Z0-9]/g, '-').toLowerCase() + '-cache-v1';
                // Proses Gambar Logo
                let iconBase64 = '';
                if (logoFile) {
                    iconBase64 = await readLogoAsBase64(logoFile);
                } else {
                    iconBase64 = generateIconBase64(appName, themeColor, iconColor);
                }
                // File 1: index.html
                const indexHtmlContent = `<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>${appName}</title>
    <link rel="manifest" href="manifest.json">
    <meta name="theme-color" content="${themeColor}">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <link rel="apple-touch-icon" href="icon-512.png">
    <style>${userCss}</style>
</head>
<body>
    ${userHtml}
    <script>
        if ('serviceWorker' in navigator) {
            window.addEventListener('load', () => {
                navigator.serviceWorker.register('sw.js')
                    .then(reg => console.log('Service Worker Aktif!'))
                    .catch(err => console.log('Service Worker Gagal:', err));
            });
        }
        ${userJs}
    <\/script>
</body>
</html>`;
                // File 2: manifest.json
                const manifestJsonContent = JSON.stringify({
                    "name": appName,
                    "short_name": appShortName,
                    "start_url": "./index.html",
                    "display": "standalone",
                    "background_color": "#ffffff",
                    "theme_color": themeColor,
                    "icons": [
                        {
                            "src": "icon-512.png",
                            "sizes": "512x512",
                            "type": "image/png",
                            "purpose": "any maskable"
                        }
                    ]
                }, null, 4);
                // File 3: sw.js
                const swJsContent = `const CACHE_NAME = '${cacheName}';
const urlsToCache = ['./', './index.html', './manifest.json', './icon-512.png'];
self.addEventListener('install', event => {
    event.waitUntil(caches.open(CACHE_NAME).then(cache => cache.addAll(urlsToCache)));
});
self.addEventListener('fetch', event => {
    event.respondWith(caches.match(event.request).then(res => res || fetch(event.request)));
});
self.addEventListener('activate', event => {
    event.waitUntil(caches.keys().then(keys => Promise.all(keys.map(k => k !== CACHE_NAME ? caches.delete(k) : null))));
});`;
                // Pembuatan file ZIP
                const zip = new JSZip();
                zip.file("index.html", indexHtmlContent);
                zip.file("manifest.json", manifestJsonContent);
                zip.file("sw.js", swJsContent);
                zip.file("icon-512.png", iconBase64, {base64: true});
                const contentBlob = await zip.generateAsync({type: "blob"});
                // --- PROSES SIMPAN KE DATABASE LOKAL ---
                await db.projects.add({
                    appName: appName,
                    zipName: zipName,
                    dateCreated: new Date().toISOString(),
                    logoBase64: iconBase64,
                    zipBlob: contentBlob
                });
                // Picu download langsung file yang baru dibuat
                saveAs(contentBlob, `${zipName}.zip`);
                // Perbarui tabel riwayat proyek
                loadArchiveTable();
                notif.innerHTML = "✅ Sukses! File ZIP berhasil diunduh dan tersimpan aman di database arsip.";
                notif.className = "mt-4 p-4 rounded-lg text-center font-medium bg-green-100 text-green-800 border border-green-200 block";
            } catch (error) {
                console.error(error);
                notif.innerHTML = "❌ Terjadi masalah saat membuat atau menyimpan file.";
                notif.className = "mt-4 p-4 rounded-lg text-center font-medium bg-red-100 text-red-800 border border-red-200 block";
            } finally {
                btn.innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 4H6a2 2 0 00-2 2v12a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-2m-4-1v8m0 0l3-3m-3 3L9 8m-5 5h2.586a1 1 0 01.707.293l2.414 2.414a1 1 0 00.707.293h3.172a1 1 0 00.707-.293l2.414-2.414a1 1 0 01.707-.293H20" /></svg> Buat, Simpan & Download PWA (.zip)`;
                btn.disabled = false;
            }
        });
        // Jalankan fungsi load data saat halaman dibuka
        window.addEventListener('DOMContentLoaded', loadArchiveTable);
    </script>
</body>
</html>
```

